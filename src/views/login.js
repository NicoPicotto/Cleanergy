import React from 'react'
import {Flex, Button, Heading, Text, Input, Divider} from "@chakra-ui/react"

const login = () => {
  return (
    <Flex>
        <Heading>Login</Heading>
    </Flex>
  )
}

export default login